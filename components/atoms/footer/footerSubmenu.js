const FooterSubmenu = ({ children }) => {
  return (
    <h4 className="font-oswald text-sm font-medium uppercase tracking-[0.5px] text-white md:text-base">
      {children}
    </h4>
  );
};

export default FooterSubmenu;
